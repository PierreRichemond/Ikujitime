<div class="upload-location">
    <%= render 'add_photos_form', event: event%>
</div>
<p class="paragraph-style">I Hope you and <span class="input-text" ><%= child.first_name %></span> had an amazing time!
  <br>On <span class="input-text" ><%= event.start_date.strftime("%B %d, %Y") %></span> you had an event, <span class="input-text"><%= event.occasion%></span>!
  <br>For <span class="input-text" ><%= child.first_name %></span> you bought a
  <% if event.gift.present? %>
    <span class="input-text"><%= event.gift.name %></span>.
  <% else %>
    <%= link_to child_path(child, tab: 'gifts'), class: "passed-dashed-box" do %>
    🎁
    <% end %>.
  <% end %>
  Then you went to
  <% if event.activity.present? %>
    <span class="input-text"><%= event.activity.name %></span> for the activity.
  <% else %>
    <%= link_to child_path(child, tab: 'activities'), class: "passed-dashed-box" do %>
    🎢
    <% end %>.
  <% end %>
    <br>If you haven't done so, there is no better way to store those memories with <span class="input-text" ><%= child.first_name %></span> by uploading 📸 them here!
</p>
<% if event.photos.any? %>
  <div class='w-100 overflow-hidden'>
    <div class="owl-carousel owl-theme align-self-center">
      <% event.photos.each do |photo| %>
        <%= cl_image_tag photo.key, width: '250', height: '250', object_fit: "cover"%>
      <% end %>
    </div>
  </div>
<% end %>
