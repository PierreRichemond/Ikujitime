<div class='show-card d-flex align-items-center'>
  <%= simple_form_for event do |f| %>
    <%= f.input :activity_id, as: :hidden, input_html: { value: @activity.id, class: 'mr-2'}, wrapper_html: { class:'m-0' }  %>
    <%= f.submit "Add", class: "btn btn-primary br-10" %>
  <% end %>
  <p class='mb-0 ml-2'><%= event.occasion.capitalize %>
    <br>
    <%= event.start_date.strftime("%B %d, %Y") %></p>
</div>
