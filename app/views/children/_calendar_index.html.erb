<%= month_calendar(events: @all_events) do |date, events| %>
  <% if events.length > 0 %>
    <% events.each do |event| %>
      <%# <% if event.start_date.strftime("%Y-%m-%d") >= Date.today.strftime("%Y-%m-%d") %>
      <div class="mark-date">
        <%= date.day %>
        <% break %>
      </div>
    <% end %>
  <% else %>
    <%= date.day %>
  <% end %>
<% end %>
